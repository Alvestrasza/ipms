# Server-only Control Plane endpoint. Never expose privileged credentials here.
IPMS_CONTROL_PLANE_URL=http://127.0.0.1:8000
# Explicit browser-facing origin for tenant selection; never derive trust from proxy headers.
# Deployment uses its canonical HTTPS origin. HTTP is limited to loopback development.
IPMS_PUBLIC_ORIGIN=http://localhost:3000
