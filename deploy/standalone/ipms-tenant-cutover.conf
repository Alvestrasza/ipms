# Persistent start fence during a principal-separation security migration.
# Only remove the marker after verifying a compatible forward release.
[Unit]
ConditionPathExists=!/srv/ipms/shared/tenant-cutover.pending
